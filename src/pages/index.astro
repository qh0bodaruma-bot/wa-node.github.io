---
import BaseLayout from '../layouts/BaseLayout.astro';
import ConsultationFlow from '../components/ConsultationFlow.astro';

const jsonLd = {
  "@context": "https://schema.org",
  "@type": "LocalBusiness",
  "name": "和-Node (wa-node)",
  "url": "https://www.wa-node.com/",
  "image": "https://www.wa-node.com/images/ogp.png",
  "description": "和-Nodeは、心理学を応用したWeb制作・導線設計の専門家です。LP・HP制作からAI・ICT活用、月額制の伴走コンサルティングまで提供します。",
  "address": {
    "@type": "PostalAddress",
    "addressCountry": "JP"
  }
};
---

<BaseLayout title="心理学×Web制作｜選ばれる導線とAI伴走でビジネスを整える｜和-Node" description="心理学の知見をWeb導線設計に応用。20年のエンジニア経験を活かしたHP・LP制作、AI・ICT活用、そして月額制の継続伴走コンサルティングで、あなたのビジネスの可能性を最大化します。">
  <!-- JSON-LD for SEO -->
  <script type="application/ld+json" is:inline set:html={JSON.stringify(jsonLd)} />

  <main>
    <!-- Hero Section -->
    <section class="section hero">
      <div class="hero-inner">
        <div class="hero-content reveal">
          <div class="hero-badge">月3名まで丁寧に対応</div>
          <p class="tagline-top">技術の前に、心がある。</p>
          <h1>心理学を応用した<br />Web制作で、<br />選ばれる導線をつくる</h1>
          <p class="lead">和-Nodeは、<strong>心理学の知見をWeb導線・相談設計・組織内コミュニケーションに応用し、</strong>ビジネスの成果と心地よさを両立させるWeb制作・伴走パートナーです。</p>
          <div class="btns">
            <a class="btn primary" href="/contact">無料で相談する</a>
            <a class="btn secondary" href="/psychology-demo/">心理導線デモを体験</a>
            <a class="btn secondary" href="/works">制作実績を見る</a>
          </div>
          <div class="hero-stats">
            <div class="stat-item">
              <span class="stat-num">20y+</span>
              <span class="stat-label">Engineering</span>
            </div>
            <div class="stat-item">
              <span class="stat-num">3k+</span>
              <span class="stat-label">Consulting</span>
            </div>
            <div class="stat-item">
              <span class="stat-num">100%</span>
              <span class="stat-label">Psychology</span>
            </div>
          </div>
        </div>
        <div class="hero-visual reveal">
          <div class="hero-avatar">
            <img src="/images/avatar.png" alt="和-Node 代表 高野紘純" />
            <div class="hero-badge-small">代表 高野 紘純</div>
          </div>
        </div>
      </div>
    </section>

    <!-- Services Section (Updated Priority) -->
    <section class="section" id="services">
      <div class="section-inner">
        <div class="section-title">
          <span class="eyebrow">Service Pillars</span>
          <h2>心理学と技術で、<br class="pc-only" />ビジネスの「出口」を整える。</h2>
        </div>
        <div class="consult-grid">
          <article class="consult-card highlight">
            <span class="card-tag">Web Design</span>
            <h3>心理導線のWeb制作</h3>
            <p>行動心理に基づいた、離脱させない情報設計。LPから多機能HPまで、20年の技術力で形にします。</p>
            <a href="/pricing" class="card-link">制作プランを見る →</a>
          </article>
          <article class="consult-card">
            <span class="card-tag">Ongoing Support</span>
            <h3>月額ICT・AI伴走</h3>
            <p>作って終わりにしない。月額制のコンサルティングで、AI活用から業務改善まで継続的にサポートします。</p>
            <a href="/pricing" class="card-link">伴走プランを見る →</a>
          </article>
          <article class="consult-card">
            <span class="card-tag">ICT & System</span>
            <h3>ICT・アプリ連携</h3>
            <p>LINE Mini App活用や、ChatGPTを用いた業務自動化。複雑な仕組みを「人に優しく」実装します。</p>
            <a href="/pricing" class="card-link">連携メニューを見る →</a>
          </article>
        </div>
      </div>
    </section>

    <!-- Psychology Demo & Works (Moved Up) -->
    <section class="section" id="works">
      <div class="section-inner">
        <div class="section-title" style="text-align: center;">
          <span class="eyebrow">Experience the Difference</span>
          <h2>「伝わる」の正体を体験・確認する</h2>
        </div>
        <div class="action-buttons-grid reveal">
          <a href="/psychology-demo/" class="action-card highlight-border">
            <div class="action-icon">💡</div>
            <h3>心理導線デモ</h3>
            <p>なぜこの配置なのか。心理学的根拠を可視化したデモページ</p>
          </a>
          <a href="/works" class="action-card">
            <div class="action-icon">📂</div>
            <h3>制作実績・サンプル</h3>
            <p>架空サンプルを含む、和-Nodeのクオリティを確認</p>
          </a>
          <a href="/simulator" class="action-card">
            <div class="action-icon">💰</div>
            <h3>料金を試算する</h3>
            <p>概算費用と納期をリアルタイム確認</p>
          </a>
        </div>
      </div>
    </section>

    <!-- Philosophy (Mental Expertise as Background) -->
    <section class="section philosophy" id="philosophy">
      <div class="section-inner">
        <div class="section-title">
          <span class="eyebrow">Background</span>
          <h2>心理学の知見を、<br class="sp-only" />実務のすべての接点に。</h2>
        </div>
        <div class="philosophy-card reveal">
          <div class="philosophy-content">
            <p>和-Nodeは、心理学を「不安を煽る道具」ではなく、<strong>「Web導線の最適化」「円滑な相談設計」「組織内コミュニケーション」</strong>に応用しています。</p>
            <p>20年のエンジニア経験と産業カウンセラーの知見を融合。<strong>「技術をカウンセラーに説明し、心理をエンジニアに説明する」というコストをゼロにします。</strong></p>
            <p>心理的安全性と実務効率を同じ窓口で解決できるからこそ、無理のない、けれど着実な一歩目を提案できます。</p>
            
            <div class="synergy-container reveal">
              <svg class="synergy-svg" viewBox="0 0 400 300" xmlns="http://www.w3.org/2000/svg">
                <circle cx="200" cy="120" r="70" fill="var(--soft)" opacity="0.6" stroke="var(--accent)" stroke-width="1"/>
                <circle cx="150" cy="200" r="70" fill="var(--soft)" opacity="0.6" stroke="var(--accent)" stroke-width="1"/>
                <circle cx="250" cy="200" r="70" fill="var(--soft)" opacity="0.6" stroke="var(--accent)" stroke-width="1"/>
                <text x="200" y="110" text-anchor="middle" font-size="14" font-weight="bold" fill="var(--accent)">心理学</text>
                <text x="200" y="130" text-anchor="middle" font-size="10" fill="var(--muted)">行動を促す・安心を創る</text>
                <text x="130" y="200" text-anchor="middle" font-size="14" font-weight="bold" fill="var(--accent)">Web制作</text>
                <text x="130" y="220" text-anchor="middle" font-size="10" fill="var(--muted)">価値を形にする</text>
                <text x="270" y="200" text-anchor="middle" font-size="14" font-weight="bold" fill="var(--accent)">ICT/AI</text>
                <text x="270" y="220" text-anchor="middle" font-size="10" fill="var(--muted)">時間を創り出す</text>
                <text x="200" y="180" text-anchor="middle" font-size="18" font-weight="800" fill="var(--accent2)">和-Node</text>
              </svg>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Ethical Declaration -->
    <section class="section ethics-section">
      <div class="container narrow">
        <div class="ethics-box reveal">
          <h2>「売るため」ではなく、「届けるため」の心理学。</h2>
          <p>
            私たちは心理学を、必要な人に正しく価値が届くための<strong>「誠実な情報設計」</strong>に活用します。不安を煽る手法とは対極にある、10年後も愛されるWebサイトをお手伝いします。
          </p>
        </div>
      </div>
    </section>

    <!-- About Profile (Refined Message) -->
    <section class="section" id="about">
      <div class="section-inner">
        <div class="about-content">
          <div class="section-title" style="text-align: center; margin-bottom: 32px;">
            <span class="eyebrow">Professional</span>
            <h2 style="font-size: clamp(1.8rem, 4vw, 2.8rem); line-height: 1.3;">なぜ「違う視点」で<br class="sp-only"/>設計できるのか</h2>
          </div>
          
          <div class="about-text-wrap reveal" style="text-align: center; max-width: 800px; margin: 0 auto;">
            <p>IT業界20年のエンジニア経験、産業カウンセラーとしての専門知見、そして車いす当事者としての視点。</p>
            <p>これらはすべて、<strong>「1pxの不親切が、どれほど人の心を遠ざけるか」</strong>を知っている強みです。技術を単なる道具ではなく、人と人を繋ぐ優しい「和（なぎ）」としてお届けするのが、私の使命です。</p>
            
            <div class="btns" style="justify-content: center; margin-top: 40px;">
              <a class="btn primary" href="/about">詳しいプロフィール・事業概要</a>
              <a class="btn secondary" href="/pricing">提供サービス一覧</a>
            </div>
          </div>
        </div>
      </div>
    </section>
    
    <!-- Tataki-dai Refined -->
    <section class="section visual-proof" style="background: var(--paper); padding-top: 80px; padding-bottom: 80px; border-top: 1px solid var(--line); border-bottom: 1px solid var(--line);">
      <div class="section-inner" style="max-width: 900px; text-align: center;">
        <span class="eyebrow" style="color: var(--accent2);">Step toward Quality</span>
        <h2 style="font-size: clamp(1.8rem, 4vw, 2.8rem); font-weight: 800; margin: 16px 0 24px; line-height: 1.3;">「実物を見てから、<br class="pc-only"/>進めるかどうか決めてください」</h2>
        <p style="font-size: 1.15rem; color: var(--text); line-height: 1.8; margin-bottom: 32px;">
          初回相談後、必要に応じて<strong>「簡易的なたたき台（構成案）」</strong>を無料で作成し、実際にお見せします。<br class="pc-only"/>
          本格的な設計・制作はご契約後に進めますが、まずは「和-Nodeと組む価値」を肌で感じていただければ幸いです。
        </p>
        <div class="btns" style="justify-content: center;">
          <a class="btn" href="#consultation-flow">無料で相談・構成案を依頼する</a>
        </div>
      </div>
    </section>

    <ConsultationFlow />
  </main>
</BaseLayout>

<style is:global>
  @import url('https://fonts.googleapis.com/css2?family=Shippori+Mincho:wght@400;700;800&display=swap');

  :root {
    --accent: #c5a059;
    --accent-dark: #b18e4a;
    --accent2: #d14d3a;
    --text: #333;
    --text-deep: #1a1a1a;
    --muted: #666;
    --line: rgba(197, 160, 89, 0.2);
    --soft: #fdfaf5;
    --white: #ffffff;
    --paper: #fdfcf9;
    --radius: 24px;
    --gold: #c5a059;
  }

  body {
    font-family: 'Shippori Mincho', serif;
    color: var(--text);
    line-height: 1.8;
    background: var(--paper);
  }

  .section { padding: 100px 0; }
  .section-inner { max-width: 1100px; margin: 0 auto; padding: 0 20px; }

  h1, h2, h3 { font-family: 'Shippori Mincho', serif; font-weight: 800; }
  h1 { font-size: clamp(2.2rem, 5vw, 4rem); line-height: 1.2; margin-bottom: 24px; color: var(--text-deep); }
  h2 { font-size: clamp(1.8rem, 4vw, 2.8rem); margin-bottom: 32px; color: var(--text-deep); }

  .tagline-top { font-size: 1.1rem; color: var(--accent); font-weight: 800; letter-spacing: 0.1em; margin-bottom: 8px; }
  .lead { font-size: 1.25rem; color: var(--muted); margin-bottom: 40px; max-width: 800px; }
  .lead strong { color: var(--accent); }

  .btns { display: flex; gap: 16px; margin-top: 40px; }
  .btn {
    padding: 18px 36px;
    border-radius: 50px;
    font-weight: 800;
    text-decoration: none;
    transition: 0.3s;
    text-align: center;
  }
  .btn.primary { background: var(--accent); color: white; box-shadow: 0 10px 20px rgba(197, 160, 89, 0.2); }
  .btn.primary:hover { background: var(--accent-dark); transform: translateY(-2px); }
  .btn.secondary { border: 1px solid var(--accent); color: var(--accent); }
  .btn.secondary:hover { background: var(--soft); }

  /* Hero Specifics */
  .hero { padding: 120px 0 80px; position: relative; overflow: hidden; background: linear-gradient(135deg, #fff 0%, #fdfcf9 100%); }
  .hero-inner { display: flex; align-items: center; justify-content: space-between; max-width: 1200px; margin: 0 auto; padding: 0 40px; gap: 60px; }
  .hero-content { flex: 1; }
  .hero-visual { flex: 0 0 400px; }
  .hero-avatar { position: relative; width: 400px; height: 400px; border-radius: 40px; overflow: hidden; box-shadow: 0 30px 60px rgba(0,0,0,0.1); }
  .hero-avatar img { width: 100%; height: 100%; object-fit: cover; }
  .hero-badge-small { position: absolute; bottom: 0; width: 100%; background: rgba(255,255,255,0.9); padding: 12px; text-align: center; font-weight: 800; color: var(--accent); }
  .hero-badge { display: inline-block; background: var(--accent2); color: white; padding: 6px 16px; border-radius: 4px; font-size: 0.85rem; font-weight: 800; margin-bottom: 24px; }
  
  .hero-stats { display: flex; gap: 40px; margin-top: 60px; }
  .stat-item { display: flex; flex-direction: column; }
  .stat-num { font-size: 1.8rem; font-weight: 800; color: var(--accent); }
  .stat-label { font-size: 0.75rem; color: var(--muted); text-transform: uppercase; letter-spacing: 0.1em; }

  /* Cards Grid */
  .consult-grid, .cards { display: grid; grid-template-columns: repeat(auto-fit, minmax(320px, 1fr)); gap: 32px; margin-top: 60px; }
  .consult-card, .card {
    background: var(--white);
    padding: 48px 40px;
    border-radius: var(--radius);
    border: 1px solid var(--line);
    transition: 0.3s;
    position: relative;
    overflow: hidden;
  }
  .consult-card:hover, .card:hover { transform: translateY(-5px); box-shadow: 0 20px 40px rgba(0,0,0,0.05); border-color: var(--accent); }
  .consult-card.highlight { border-color: var(--accent); background: var(--soft); }
  .card-tag { font-size: 0.75rem; font-weight: 800; color: var(--accent); text-transform: uppercase; letter-spacing: 0.1em; margin-bottom: 16px; display: block; }
  .consult-card h3, .card h3 { font-size: 1.5rem; margin-bottom: 16px; color: var(--text-deep); }
  .consult-card p, .card p { font-size: 1rem; color: var(--muted); margin-bottom: 24px; }

  /* Action Buttons Grid */
  .action-buttons-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 24px;
    margin-top: 40px;
  }
  .action-card {
    background: #fff;
    border: 1px solid var(--line);
    border-radius: 20px;
    padding: 40px 30px;
    text-decoration: none;
    text-align: center;
    transition: 0.3s ease;
    display: block;
    color: inherit;
  }
  .action-card:hover {
    transform: translateY(-5px);
    border-color: var(--accent);
    box-shadow: 0 15px 35px rgba(197, 160, 89, 0.1);
  }
  .action-card.highlight-border { border: 2px solid var(--accent); }
  .action-icon {
    font-size: 2.5rem;
    margin-bottom: 20px;
    display: block;
  }
  .action-card h3 { font-size: 1.25rem; margin-bottom: 12px; color: var(--accent-dark); }
  .action-card p { font-size: 0.9rem; color: var(--muted); margin: 0; }

  /* Section Titles */
  .section-title { margin-bottom: 40px; }
  .eyebrow { font-size: 0.85rem; font-weight: 800; color: var(--accent); text-transform: uppercase; letter-spacing: 0.2em; margin-bottom: 12px; display: block; }

  /* Philosophy Card */
  .philosophy-card { background: var(--soft); border-radius: var(--radius); padding: 80px 60px; border: 1px solid var(--line); }
  .philosophy-content { max-width: 800px; margin: 0 auto; }
  .philosophy-content p { font-size: 1.15rem; margin-bottom: 24px; color: #444; }

  /* Ethical Box */
  .ethics-box { background: white; padding: 48px; border-radius: var(--radius); border: 1px solid var(--line); text-align: center; }

  .card-link { display: inline-block; margin-top: 24px; font-weight: 800; color: var(--accent); text-decoration: none; border-bottom: 1px solid var(--accent); }

  /* Visual Proof Section */
  .visual-proof { position: relative; overflow: hidden; }
  .visual-proof::before {
    content: "Professional"; position: absolute; top: 10px; right: -40px; background: var(--accent); color: white;
    font-size: 0.7rem; font-weight: 800; padding: 4px 30px; transform: rotate(45deg);
  }

  @media (max-width: 768px) {
    .hero { padding: 60px 0; text-align: center; }
    .hero-inner { flex-direction: column; gap: 32px; }
    .hero-visual { order: -1; flex: 0 0 auto; }
    .hero-avatar { width: 200px; height: 200px; }
    .lead { margin-inline: auto; }
    .hero-stats { justify-content: center; flex-wrap: wrap; }
    .consult-grid, .action-buttons-grid { grid-template-columns: 1fr; }
    .philosophy-card { padding: 32px 20px; }
  }

  .synergy-container {
    margin-top: 60px;
    display: flex;
    justify-content: center;
    align-items: center;
  }
  .synergy-svg {
    max-width: 500px;
    width: 100%;
    height: auto;
  }
</style>

<script is:inline>
  function initParallax() {
    window.addEventListener('scroll', () => {
      const scrolled = window.pageYOffset;
      const heroVisual = document.querySelector('.hero-visual');
      if (heroVisual) {
        heroVisual.style.transform = `translateY(${scrolled * 0.1}px)`;
      }
    });
  }
  document.addEventListener('astro:page-load', initParallax);
</script>
