# 和-Node 作業ルール（全体共通）

## このリポジトリの性格

公開Webサイト（wa-node.com）のソースと、経営ドキュメントが同居している。

**GitHubリポジトリは public 設定。** `business-docs/private/` だけが `.gitignore` 済みで非公開。
金額・顧客名・売上実績を書くファイルは必ず `business-docs/private/` の中に置く。それ以外の場所には絶対に書かない。

## 出力の絶対ルール

### 1. 事実と推測を分ける

数字や状況に言及するときは、必ず次のいずれかを頭に付ける。

- `【事実】` — リポジトリ内のファイルに根拠がある
- `【推測】` — 根拠がなく、こちらの推論
- `【要確認】` — 判断に必要だが、リポジトリ内にデータが無い

### 2. 数字には出典を付ける

金額・件数・比率を出すときは `（出典: business-docs/private/actuals-2026.md）` の形でファイル名を併記する。
出典を示せない数字は書かない。「だいたい○円くらい」で埋めない。

### 3. 計画値を実績として語らない

`business-docs/business-plan.html` と `private/plan-summary.md` は**すべて計画値**（2026年5月作成）。
実績は `private/actuals-2026.md` にしかない。未記入の月は「実績データなし」と明記する。

### 4. 経営提案には必ず反対材料を付ける

施策を提案するときは、同じ回答の中に「うまくいかない条件」と「やらない場合の選択肢」を最低1つ書く。
賛成だけの回答は不可。一人事業で、他に反対する人間がいないため。

### 5. 金額は税別で統一

税込を書くときだけ明示する。

### 6. 税務・法務・医療の断定をしない

計算の材料と根拠は出すが、最終判断は税理士・専門家の確認を前提として明記する。

## 成果物の保存先

| 種類 | 置き場所 |
| --- | --- |
| 経営の分析・試算 | `business-docs/private/` |
| 意思決定の記録 | `business-docs/private/decisions/YYYY-MM-DD-件名.md` |
| 記事・LP・X の草稿 | `x-drafts/` |
| サイト実装 | `src/` |
| 使い捨ての作業ファイル | スクラッチパッド（リポジトリに残さない） |

## 役割別ルール

`business-docs/roles/` 配下に3つ。作業内容に応じて該当フォルダの `CLAUDE.md` を読んでから着手する。

- `management/` — 数字・資金繰り・単価・意思決定
- `marketing/` — 集客、LP・記事・SNS
- `production/` — 制作・技術・自動化

## 反対役

経営判断を固める前に `hantai-yaku` サブエージェントで検証する（定義: `.claude/agents/hantai-yaku.md`）。
ユーザーが「反対意見」「壁打ち」「これで進めていいか」と言った場合は、このエージェントを使う。
